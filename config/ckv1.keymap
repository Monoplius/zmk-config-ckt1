#include <behaviors.dtsi>
#include <dt-bindings/zmk/keys.h>
#include <dt-bindings/zmk/ext_power.h> // Added this line

/ {
  behaviors {
    hp_pwr: hold_pwr {
      compatible = "zmk,behavior-hold-tap";
      label = "HOLD_PWR";
      #binding-cells = <2>;
      tapping-term-ms = <1000>;
      quick-tap-ms = <0>;
      bindings = <&kp NO>, <&ext_power EP_TOG>; // Replaced NONE with NO
    };
    hp_rst: hold_rst {
      compatible = "zmk,behavior-hold-tap";
      label = "HOLD_RST";
      #binding-cells = <2>;
      tapping-term-ms = <1000>;
      quick-tap-ms = <0>;
      bindings = <&kp NO>, <&bootloader>; // Replaced NONE with NO
    };
  };

  keymap {
    compatible = "zmk,keymap";
    default_layer {
      bindings = <
        // Left Half (QWERTZ)
        &kp Q,    &kp W,    &kp E,    &kp R,    &kp T,    &kp Z,
        &kp A,    &kp S,    &kp D,    &kp F,    &kp G,    &kp H,
        &kp Y,    &kp X,    &kp C,    &kp V,    &kp B,    &kp N,
        &mo 1,    &kp LCTL, &kp LALT, &kp SPACE, &kp TAB,  &kp ESC,

        // Right Half
        &kp U,    &kp I,    &kp O,    &kp P,    &kp LBKT, &kp RBKT,
        &kp J,    &kp K,    &kp L,    &kp SEMI, &kp QUOT, &kp BSLH,
        &kp M,    &kp COMM, &kp DOT,  &kp SLASH, &kp MINUS, &kp EQUAL,
        &kp DEL,  &kp INS,  &kp BSPC, &kp ENTER, &kp LSFT, &trans
      >;
    };

    layer_1 {
      bindings = <
        // Left Num/System
        &hp_pwr NUM_1 EP_TOG,    &hp_rst NUM_2 BOOTLOADER,
        &kp ▋
